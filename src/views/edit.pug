extends base.pug


block content 
    h4 Update video
    form(method="POST")
        input(name="title", value=video.title, required)
        input(name="description", value=video.description, required, type="text", minlength=20)
        input(name="hashtags", ,value=video.hashtags.join(), required, type="text")
        input(type="submit", value="save")